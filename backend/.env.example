# Copy to .env and fill in. NEVER commit the real .env.
# Ollama runs locally on the same machine as this backend.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b

# Optional overrides
PORT=8787
